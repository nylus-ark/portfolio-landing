import React from 'react';
import { Element } from 'react-scroll';

import './contacts.css';

export const Contacts = () => {
    return (
        <Element 
            className="contacts" 
            name="contacts">
            Contacts
        </Element>
    );
}
