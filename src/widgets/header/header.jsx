import React from 'react';
import { Element } from 'react-scroll';
import './header.css';

export const Header = () => {
    return (
        <Element 
            className="header"
            name="home">
            Header
        </Element>
    );
}
