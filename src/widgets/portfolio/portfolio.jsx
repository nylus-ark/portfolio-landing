import React from 'react';
import { Element } from 'react-scroll';

import './portfolio.css';

export const Portfolio = () => {
    return (
        <Element 
            className="portfolio" 
            name="portfolio">
            Portfolio
        </Element>
    );
}
